import React from 'react';

const UserOutput = (props) => {
    return(
        <div>
            <p>repeat</p>
            <p>There is {props.constel}</p>
        </div>
    );
}

export default UserOutput;